import React from 'react';
import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const DealArchive = ({theme}) => {

  return(
    <SafeAreaView>
      <Text>A R C H I V E</Text>
    </SafeAreaView>
  );
}

export default DealArchive;
