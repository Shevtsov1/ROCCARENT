import React from 'react';
import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const UserListings = ({theme}) => {

  return(
    <SafeAreaView>
      <Text>U S E R   L I S T I N G S</Text>
    </SafeAreaView>
  );
}

export default UserListings;
